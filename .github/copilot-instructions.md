# LZT Market Bot - Инструкции для разработки

## 📋 Описание проекта

Профессиональный бот для поиска объявлений и проверки соответствия правилам LZT Market. Поддерживает 7 режимов работы:
- Поиск по ключевым словам
- Автоматическая проверка всех объявлений
- Проверка разделов по правилам
- Проверка неверного происхождения
- Поиск поддельных личных аккаунтов
- Поиск отлеги по годам в Telegram
- Поиск Social Club в Steam и Epic Games

**Язык:** JavaScript (Node.js)  
**Версия Node.js:** >=26.3.0  
**Тип модуля:** ES Modules (type: "module")  
**Версия проекта:** 3.1.0  
**Лицензия:** MIT

## 🛠️ Технический стек

- **Runtime:** Node.js 26.3.0 (встроенные API: fetch, readline, fs, path, timers/promises)
- **Асинхронность:** async/await, Promise
- **Файловая система:** fs (встроенный)
- **Сетевые запросы:** fetch API (встроенный в Node.js 26.3.0)
- **CLI:** readline (встроенный)
- **Тестирование:** assert (встроенный)
- **Модули:** ES Modules (не CommonJS)

## 📁 Структура проекта

```
lzt-market-bot/
├── bot.js                        # Основной файл бота (~1400 строк)
├── inspect_paths.js              # Утилита для проверки путей API (ES Modules)
├── config.example.json           # Пример конфигурации
├── config.json                   # Конфиг (исключён из git)
├── rules.json                    # Правила нарушений
├── package.json                  # Зависимости и скрипты
├── package-lock.json             # Хеш зависимостей
├── .gitignore                    # Исключённые файлы
├── README.md                     # Документация пользователя
├── SUMMARY.md                    # Краткий обзор функций
├── AUDIT.md                      # История проверок и изменений
├── .github/
│   └── copilot-instructions.md   # Этот файл
├── test/
│   └── bot.test.js               # Юнит-тесты (assert, ~50 строк)
└── bot.log / errors.log          # Логи (не в git)
```

## 🚀 Быстрый старт

### 1. Установка
```bash
# Убедитесь, что установлен Node.js 26.3.0 или выше
node --version
```

### 1.1 Установка зависимостей
```bash
npm install
```

### 2. Конфигурация
```bash
cp config.example.json config.json
# Отредактируйте config.json и добавьте ваш API token
```

### 2.1 Важные параметры config.json
- `token` — Bearer token для API LZT Market.
- `proxyUrl` — URL HTTPS-прокси для исходящих API-запросов. Если пусто, используется `HTTPS_PROXY` / `https_proxy` / `HTTP_PROXY` / `http_proxy`.
- `apiBaseUrl` — основной API-хост (`https://prod-api.lzt.market`).
- `apiAlternateUrl` — резервный API-хост на случай сетевых ошибок.
- `maxPages` и `resultsPerPage` — контролируют глубину поиска и размер страницы.
- `maxRetries` и `retryDelayMs` — управление повторными попытками при 429 и сетевых ошибках.
- `parallelProcessing` и `maxConcurrentRequests` — включают параллельную обработку страниц и ограничивают количество одновременных запросов.
- `enableWebServer` / `webPort` — запуск встроенного веб-интерфейса.
- `telegramToken` / `telegramChatId` — уведомления в Telegram.

### 2.2 Резервный прокси и сетевые настройки
- `proxyUrl` имеет приоритет над переменными окружения.
- Если прокси не задан, бот автоматически учитывает стандартные переменные окружения `HTTPS_PROXY`, `https_proxy`, `HTTP_PROXY`, `http_proxy`.
- `apiAlternateUrl` используется как fallback при сетевых ошибках, связанных с TLS, Cloudflare или отсутствием доступа к основному хосту.

### 3. Запуск
```bash
npm start              # Запустить бота в интерактивном режиме
npm test               # Запустить тесты
npm run check-syntax   # Проверить синтаксис всех файлов
npm run lint           # Запустить linter
```

### 3.1 NPM-скрипты
- `npm start` — запускает `bot.js`.
- `npm test` — последовательно выполняет `test/bot.test.js`, `test/input_parsing.test.js`, `test/order_parsing.test.js`.
- `npm run check-syntax` — запускает Node в режиме `--check` для всех основных файлов.
- `npm run lint` — выполняет `check-syntax`, затем ESLint по всему проекту.

## 📖 Ключевые функции

### 7 режимов работы

| № | Режим | Описание |
|---|-------|---------|
| 1 | Поиск по ключевым словам | Поиск объявлений по фразам с проверкой нарушений |
| 2 | Автоматическая проверка | Проверка всех объявлений в категории |
| 3 | Проверка разделов | Проверка выбранных категорий, сводная таблица |
| 4 | Проверка происхождения | Ищет несоответствие названия и происхождения |
| 5 | Поддельные личные | Ищет "личный" с неправильным происхождением |
| 6 | Отлеги в Telegram | Поиск отложки по годам в категории Telegram |
| 7 | Social Club поиск | Поиск GTA/RDR без Social Club в Steam/Epic |

### Проверка нарушений

- **vague_aging** (50+ ключевых слов): отлежка, неактивный, спящий, древний, заброшенный и т.д.
- **overhype** (40+ ключевых слов): топ, крутой, жир, бомба, огонь, супер, элита и т.д.

### API интеграция

- Базовый URL: `https://prod-api.lzt.market`
- Аутентификация: Bearer token
- Обработка 429 (rate limit) с экспоненциальной задержкой
- Повторные попытки при сетевых ошибках

## 🔧 Основные функции

### Экспортируемые функции (используются в тестах)

```javascript
// Нормализация текста для поиска
normalizeText(text) → String

// Проверка нарушений в тексте
checkViolations(text, rules) → Array<{category, name, keyword}>

// Получение ID объявления (поддерживает item_id, id, uid)
getItemId(item) → String

// Валидация конфигурации
validateConfig(config) → void (throws Error)

// Проверка явной даты/периода отлежки
hasExplicitAgeOrDateNearKeyword(textLower, keywordLower) → Boolean
```

### Внутренние функции (только для бота)

```javascript
// Загрузка конфигурации
loadConfig() → Object

// Загрузка правил нарушений
loadRules() → Object

// Поиск объявлений на одной странице
async searchOnce(config, rules, page) → {results, rawCount}

// Сбор объявлений со всех страниц
async collectPages(config, rules, itemLabel) → Array

// Сбор по фразам (для режимов 6-7)
async collectPhraseSearches(config, rules, phrases, itemLabel) → Array

// Обработчики режимов
async searchByKeywords(config, rules)
async autoCheckAllListings(config, rules)
async checkAllCategories(config, rules, ask)
async searchFakePersonal(config, rules, ask)
async checkAllOrigins(config, rules, ask)
async searchTelegramOtlegYears(config, rules)
async searchSocialClubAccounts(config, rules)
```

## 🔐 Безопасность

### 1. Управление токенами
- ✅ config.json исключён из git (.gitignore)
- ✅ Используйте config.example.json как шаблон
- ✅ Никогда не коммитьте реальные токены

### 2. Обработка ошибок
- ✅ Try-catch для всех API запросов
- ✅ Graceful shutdown при Ctrl+C
- ✅ Логирование ошибок в errors.log

### 3. Валидация входных данных
- ✅ validateConfig() проверяет все параметры
- ✅ filterValidOrigins() валидирует фильтры

## 📊 Оптимизации

### Rate Limiting
- ✅ Экспоненциальная задержка: 500ms, 1s, 2s, 4s...
- ✅ Максимум 2 повтора при 429
- ✅ Сброс счётчика при успешном запросе

### Дедупликация
- ✅ getItemId() поддерживает item_id, id, uid
- ✅ uniqItemsById() удаляет дубликаты
- ✅ Optional chaining (?.) для безопасного доступа

### Память
- ✅ mergeResults() вместо concat() - семантика яснее
- ✅ Потоковая обработка результатов
- ✅ Очистка данных после вывода

## 🧪 Тестирование

### Запуск тестов
```bash
npm test
```

### Что тестируется
- ✅ normalizeText() - нормализация текста
- ✅ checkViolations() - проверка нарушений
- ✅ getItemId() - получение ID объявления
- ✅ validateConfig() - валидация конфигурации
- ✅ hasExplicitAgeOrDateNearKeyword() - проверка явной даты

## 📝 Логирование

### bot.log
- Все события: запуски, успешные запросы, повторы

### errors.log
- Ошибки: HTTP ошибки, сетевые ошибки, невалидные данные

## 💡 Лучшие практики

### 1. Код
- Используйте ES Modules (import/export)
- Предпочитайте const и let вместо var
- Используйте optional chaining (?.) и nullish coalescing (??)
- Экспортируйте тестируемые функции

### 2. Конфигурация
- Не коммитьте config.json
- Используйте разумные defaults в config.example.json
- Документируйте каждый параметр комментариями

### 3. Асинхронность
- Всегда используйте try-catch для await
- Проверяйте isInterrupted в длинных циклах
- Добавляйте задержки между запросами

### 4. Тестирование
- Тесты запускаются перед коммитом
- Все экспортируемые функции должны быть протестированы
- Используйте assert для проверок

## 🐛 Отладка

### Включить подробное логирование
```bash
# Отредактируйте bot.js и раскомментируйте console.log()
console.log('DEBUG:', data);
```

### Проверить синтаксис
```bash
npm run check-syntax
```

### Посмотреть логи
```bash
tail -f bot.log
tail -f errors.log
```

## 📚 Документация

- [README.md](../README.md) - Руководство пользователя
- [AUDIT.md](../AUDIT.md) - История версий и оптимизаций
- [SUMMARY.md](../SUMMARY.md) - Краткий обзор
- config.example.json - Пример конфигурации

## 🔄 Обновление версий

**Текущая версия:** 3.1.0

Чтобы обновить:
1. Обновите `version` в `package.json`
2. Добавьте запись в `AUDIT.md`
3. Сделайте коммит: `git commit -m "v3.1.0: описание"`

## ✅ Чек-лист перед push

- [ ] `npm run check-syntax` проходит без ошибок
- [ ] `npm test` проходит полностью
- [ ] `config.json` исключён из коммита
- [ ] Обновлены `AUDIT.md` и `README.md`, если нужно
- [ ] Нет `console.log()` в production-коде
- [ ] Все новые функции экспортированы и протестированы

## 🐛 Частые ошибки и отладка

### Проблемы с прокси
- Если API недоступен из-за Cloudflare или TLS, используйте `proxyUrl` или установите `HTTPS_PROXY` / `https_proxy`.
- `proxyUrl` имеет приоритет над переменными окружения.
- Пример: `"proxyUrl": "http://127.0.0.1:8080"`.

### Ошибки API и 429
- При `429` бот делает повторные попытки с экспоненциальной задержкой.
- Увеличьте `retryDelayMs` или `maxRetries`, если часто получаете `rate limit`.

### Проверка синтаксиса и логов
- Запустите `npm run check-syntax` для валидации JavaScript.
- Проверяйте `bot.log` и `errors.log` в корне проекта.

### Отладка кода
```bash
# Временно добавьте console.log в bot.js или api.js
console.log('DEBUG:', data);
```

### Запуск бота
```bash
npm start
```

### Запуск тестов
```bash
npm test
```

## 📝 Скрипты npm

| Команда | Описание |
|---------|---------|
| `npm start` | Запускает основной бот |
| `npm test` | Запускает юнит-тесты |
| `npm run check-syntax` | Проверяет синтаксис JavaScript |
| `npm run lint` | Проверяет синтаксис и запускает ESLint |

## 🎯 Режимы работы

### Режим 1: Поиск по ключевым словам
- Ищет объявления по фразам из конфига или пользовательским параметрам
- Поддерживает фильтры по цене, происхождению, категориям
- **Использование:** Поиск конкретных объявлений

### Режим 2: Автоматическая проверка всех объявлений
- Проверяет все объявления в категории на нарушения правил
- Показывает только объявления с найденными нарушениями
- **Использование:** Поиск запрещённого контента

### Режим 3: Проверка разделов по правилам
- Проверяет несколько выбранных категорий на нарушения
- Агрегирует результаты в одну таблицу
- **Использование:** Мониторинг нескольких разделов

### Режим 4: Проверка происхождения
- Ищет несоответствие названия и происхождения
- Показывает объявления, в которых указано одно происхождение, но выбрано другое

### Режим 5: Поддельные личные аккаунты
- Ищет объявления с `личный` в названии, но другим происхождением
- Помогает выявить обман при продаже

### Режим 6: Отлеги в Telegram
- Ищет объявления с указанием давности аккаунта (2–12 лет)
- Специально для категории Telegram

### Режим 7: Social Club поиск
- Ищет GTA/RDR аккаунты без Social Club в Steam и Epic Games

- Выявляет обман при продаже
- **Использование:** Защита от мошенничества
- **Использование:** Защита от мошенничества

## 🔧 Основные функции

### Проверка нарушений
- **checkViolations(text, rules)** - проверяет текст на нарушения правил
- **hasExplicitAgeOrDateNearKeyword()** - игнорирует нарушения "отлежка" если есть дата
- **normalizeText()** - нормализует текст для поиска

### Поиск и обработка
- **searchOnce()** - выполняет один поисковый запрос к API
- **collectPages()** - собирает результаты со нескольких страниц
- **formatItem()** - форматирует результат в стандартный вид

### Отображение результатов
- **displayResults()** - показывает все результаты с пагинацией
- **displayViolationsOnly()** - показывает только результаты с нарушениями
- **finalizeResults()** - выводит итоговую статистику

### API и сетевые функции
- **fetchJson()** - выполняет GET запрос с автоматическими повторами при 429
- **loadConfig()** - загружает и валидирует конфигурацию
- **loadRules()** - загружает правила нарушений

## 🔐 Безопасность

- Никогда не коммитьте `config.json` с реальным токеном
- API токен хранится в переменной окружения или в локальном config.json
- Все логи ошибок записываются в `errors.log`

## 📊 Логирование

- **bot.log** - информационные логи (поиски, результаты)
- **errors.log** - логи ошибок (неудачные запросы, исключения)
- Оба файла содержат временные метки (ISO 8601)

## 🧪 Тестирование

Проект включает базовые юнит-тесты для:
- `normalizeText()` - нормализация текста
- `checkViolations()` - проверка нарушений
- `getItemId()` - получение ID товара
- `validateConfig()` - валидация конфигурации

Запуск: `npm test`

## 🔄 Graceful Shutdown

При нажатии Ctrl+C бот:
1. Прерывает текущий поиск
2. Выводит уже найденные результаты
3. Выводит статистику (время, количество нарушений)
4. Корректно завершает работу

## 🐛 Обработка ошибок

- **429 Rate Limit** - автоматические повторы с экспоненциальной задержкой
- **Сетевые ошибки** - повторные попытки с настраиваемой задержкой
- **HTTP 404** - уведомление о неподдерживаемой категории
- **Синтаксические ошибки в config/rules** - информативные сообщения об ошибке

## 📦 Зависимости

**Нет внешних зависимостей!** Проект использует только встроенные модули Node.js:
- `fs` - файловая система
- `path` - работа с путями
- `readline` - интерактивный CLI
- `timers/promises` - асинхронные задержки
- `assert` - юнит-тесты
- `url` - обработка URL (import.meta.url)
- Встроенный `fetch` для HTTP запросов (Node.js 26.3.0)

## 🎨 Стиль кода

- **Форматирование:** 2 пробела для отступов
- **Именование:** camelCase для переменных/функций, UPPER_CASE для констант
- **Комментарии:** Русский язык с примерами
- **Пустые строки:** Для разделения логических блоков
- **Длина строк:** ~120 символов максимум

## 📝 Документирование

Все экспортируемые функции имеют JSDoc комментарии в конце файла. Функции принимают параметры:
- `config` - объект конфигурации
- `rules` - объект с правилами
- `ask` - функция для интерактивного ввода

## 🔄 Версионирование

- **Текущая версия:** 2.0.0
- **Поддержка Node.js:** 26.3.0
- **Совместимость:** Полная совместимость с современными браузерными API (fetch, Promise, async/await)
